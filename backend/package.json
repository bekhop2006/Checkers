{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "partykit dev",
    "deploy": "partykit deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@checkers/engine": "*",
    "@checkers/shared-types": "*",
    "partykit": "^0.0.110"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
