# Backend

- **PartyKit** — realtime multiplayer (`npm run dev`)
- **Supabase** — SQL migrations в `supabase/migrations/`

```bash
# из корня monorepo
npm run dev:backend
npm run deploy -w backend
```
