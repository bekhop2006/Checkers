# Frontend

Next.js 15 — доска, auth, leaderboard, Stripe, AI Coach UI.

```bash
# из корня monorepo
npm run dev:frontend

# или из этой папки
npm run dev
```

Переменные окружения: `.env` в **корне** репозитория или `frontend/.env.local`.
